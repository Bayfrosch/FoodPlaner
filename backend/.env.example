# Datenbank URL für Prisma
# Beispiel für PostgreSQL:
# DATABASE_URL="postgresql://username:password@localhost:5432/foodplaner"
# Beispiel für SQLite (Development):
# DATABASE_URL="file:./prisma/dev.db"
DATABASE_URL="postgresql://user:password@localhost:5432/foodplaner"

# Server-Port
PORT=3001

# JWT Secret für Token-Authentifizierung (ÄNDERN SIE DIES IN DER PRODUKTION!)
JWT_SECRET=your_jwt_secret_here_change_this_in_production
